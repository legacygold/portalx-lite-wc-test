# PortalX LITE WalletConnect Test

Minimal test repo for WalletConnect + Lobstr using StellarWalletsKit.

## What it does
- Connect to Lobstr via WalletConnect (QR / deep link)
- Test placing signed txs (later)
